[project]
name = "vulnapps"
version = "0.1.0"
description = "Vulnerability registry and scanner accuracy tracker"
requires-python = ">=3.8"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
